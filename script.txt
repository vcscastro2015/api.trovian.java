Agora vamos fazer um CRUD do Objeto Local na estrutura bem definida do projeto java
Vamos ter os seguinte atributos:
     private Integer id;
    @Basic(optional = false)
    @NotNull
    @Size(min = 1, max = 255)
    @Column(name = "nome")
    private String nome;
	 @NotNull
    @Column(name = "ativo")
    private Boolean ativo;
	private Integer funcao;
    @Column(name = "mostrar_mapa_principal")
    private Boolean mostrarNoMapaPrincipal;
    @Column(name = "mostrar_nome_mapa")
    private Boolean mostrarNomeNoMapa;
    @Column(name = "notifica_evento")
    private Boolean notificaEvento;
    @Column(name = "codigo_unico")
    private Integer codigoUnico;
    @Column(name = "funcao")
    private Integer funcao//Carga, Descarga ou Outros;
    @Column(name = "endereco")
    private String endereco;
    @Column(name = "bairro")
    private String bairro;
    @Column(name = "complemento")
    private String complemento;
    @Column(name = "cidade")
    private String cidade;
    @Column(name = "uf")
    private String uf;
    @Column(name = "tipo")
    private String tipo;
    @Column(name = "permite_descanso")
    private Boolean permiteDescanso;
    @JoinColumn(name = "cliente", referencedColumnName = "id")
    @ManyToOne(optional = false)
    private Cliente cliente;
    private List<Coordenada> listaDeCoordenadas;
    private ParametroLocal parametroLocal;
	
	Agora a estrutura Coordenada
	
	private Integer id;
    @Column(name = "sequencia")
    private Integer sequencia;
    @Column(name = "latitude")
    private Double latitude;
    @Column(name = "longitude")
    private Double longitude;
    @Column(name = "is_raio")
    private Boolean isRaio;
    @Column(name = "raio")
    private Double raio;
	
	Agora a estrutura de ParametroLocal
	
	private Integer id;
   

    @Column(name = "limite_veiculos_mesmo_local")
    private Integer limiteVeiculosMesmoLocal;
	private Integer tempoMinimoDePermanencia;
	private Integer tempoMaximoDePermanencia;
	
o metodo GetMapping ter somente esses:
Um para buscar todos os Veiculos com paginação
Um para buscar o Veiculo pelo id
Um para buscar os Veiculos pelo cliente
Nao precisa criar serviços rest para ParametroLocal e Coordenada, uma vez que serao gravados juntos com o local.


o Campo  private Integer funcao tera somente essas opçoes Carga, Descarga ou Outros, deixar como enum;
o Campo  private Integer tipo tera somente essas opçoes Empresa, Oficina, Posto de abastecimento, posto de fiscalização, deixar como ENUM;

Campos Obrigatorios: id, nome e status

Criar toda a infra necessaria, desde o modelo ate o rest baseado na operação Product
